#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 *main - Prints alphabet in small letters
 *Return:Always 0 (success)
 */
int main(void)
{
char ch;
for (ch = 'a'; 'e' < ch > 'q' ; ch++)
{
putchar (ch);
}