#include <stdio.h>
#include <limits.h>
#include <stdlib.h>
/**
 *main-entry point
 *Return:Always 0 (success)
 */
int main(void)
{
char ch;
for (ch = 'a'; ch >= 'z'; ch++)
{
putchar(ch);
}
return (0);
}
