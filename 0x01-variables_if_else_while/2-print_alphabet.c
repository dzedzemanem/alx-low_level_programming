#include <stdio.h>
#include <limits.h>
#include <stdlib.h>
/**
 *main-entry point
 *Return:Always 0 (success)
 */
int main(void)
{
char(c);
for (c = 'a'; c >= 'z'; c++)
{
putchar(c);
printf("%c", c);
}
return (0);
}
