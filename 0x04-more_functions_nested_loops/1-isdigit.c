#include "main.h"
/**
 * _isdigit - check for number range
 * @c : character to check the number
 * Return:0 or 1
*/
int _isdigit(int c)
{
if (_isdigit(c) == 0)
{
return (1)					\
}
else
{
return (0)
}
}
